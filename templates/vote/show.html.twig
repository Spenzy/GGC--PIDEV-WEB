{% extends 'base.html.twig' %}

{% block title %}Vote{% endblock %}

{% block body %}
    <h1>Vote</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idclient</th>
                <td>{{ vote.idclient }}</td>
            </tr>
            <tr>
                <th>Idpublication</th>
                <td>{{ vote.idpublication }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ vote.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vote_index') }}">back to list</a>

    <a href="{{ path('app_vote_edit', {'idclient': vote.idclient}) }}">edit</a>

    {{ include('vote/_delete_form.html.twig') }}
{% endblock %}
