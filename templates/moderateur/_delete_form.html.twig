<form method="post" action="{{ path('app_moderateur_delete', {'idModerateur': moderateur.idModerateur}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ moderateur.idModerateur) }}">
    <button class="btn">Delete</button>
</form>
