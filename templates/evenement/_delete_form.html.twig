<form method="post" action="{{ path('delete', {'reference': evenement.reference}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.reference) }}">
    <button class="btn">Delete</button>
</form>
