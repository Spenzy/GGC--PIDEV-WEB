<form method="post" action="{{ path('app_avis_delete', {'idavis': avi.idavis } ) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avi.idavis) }}">
    <button class="btn btn-outline-primary fa fa-trash-alt rounded-pill px-3 py-2"></button>
</form>
