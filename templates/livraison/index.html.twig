{% extends 'base.html.twig' %}

{% block title %}Livraison index{% endblock %}

{% block body %}
    <h1>Livraison index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Idcommande</th>
                <th>Idlivreur</th>
                <th>Dateheure</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for livraison in livraisons %}
            <tr>
                <td>{{ livraison.idcommande }}</td>
                <td>{{ livraison.idlivreur }}</td>
                <td>{{ livraison.dateheure ? livraison.dateheure|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_livraison_show', {'idcommande': livraison.idcommande}) }}">show</a>
                    <a href="{{ path('app_livraison_edit', {'idcommande': livraison.idcommande}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_livraison_new') }}">Create new</a>
{% endblock %}
