<form method="post" action="{{ path('app_streamer_delete', {'idstreamer': streamer.idstreamer.idPersonne}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ streamer.idstreamer) }}">
    <button class="btn">Delete</button>
</form>
